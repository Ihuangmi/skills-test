services:
  - type: web
    name: chat-server
    runtime: node
    buildCommand: npm install -g pnpm && pnpm install
    startCommand: pnpm --filter server exec tsx src/index.ts
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001