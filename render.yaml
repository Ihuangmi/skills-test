services:
  - type: web
    name: chat-server
    runtime: node
    buildCommand: npm install -g pnpm && pnpm install && pnpm --filter server build
    startCommand: node packages/server/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001